<?php
/** @var \Quiz\Entity\Quiz $quiz */
/** @var \Quiz\Entity\Customer $customer */
/** @var array $invoiceData */

$this->headTitle('Factuur - ' . $quiz->getName());
?>

<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quizis Factuur - <?php echo $this->escapeHtml($quiz->getName()); ?></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.5;
      color: #1f2937;
    }

    .page-container {
      min-height: 100vh;
      background: #f9fafb;
      padding: 2rem;
    }

    .invoice-page {
      width: 210mm;
      min-height: 297mm;
      background: white;
      box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
      margin: 0 auto;
      padding: 4rem;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 3rem;
    }

    .company-info {
      flex: 1;
    }

    .company-info h2 {
      font-size: 1.125rem;
      font-weight: 600;
      color: #c95b1f;
      margin-bottom: 0.75rem;
    }

    .logo {
      width: 12rem;
      margin-bottom: 1.5rem;
    }

    .company-details {
      font-size: 0.875rem;
      line-height: 1.6;
    }

    .company-details p {
      margin-bottom: 0.25rem;
    }

    .company-details .font-semibold {
      font-weight: 600;
    }

    .company-details .mt-3 {
      margin-top: 0.75rem;
    }

    .company-details .mt-2 {
      margin-top: 0.5rem;
    }

    .invoice-title-section {
      flex: 1;
      text-align: right;
    }

    .invoice-title {
      font-size: 3rem;
      font-weight: 700;
      color: #c95b1f;
      margin-bottom: 1.5rem;
    }

    .invoice-meta {
      font-size: 0.875rem;
      line-height: 1.8;
    }

    .invoice-meta div {
      margin-bottom: 0.5rem;
    }

    .invoice-meta .font-semibold {
      font-weight: 600;
    }

    .invoice-meta span + span {
      margin-left: 0.5rem;
    }

    .invoice-meta-row {
      display: flex;
      gap: 2rem;
      margin-bottom: 2rem;
      font-size: 0.875rem;
      padding-bottom: 1rem;
      border-bottom: 2px solid #e89264;
    }

    .invoice-meta-row > div {
      display: flex;
      gap: 0.5rem;
    }

    .invoice-meta-row .font-semibold {
      font-weight: 600;
    }

    .bill-to {
      margin-bottom: 3rem;
    }

    .bill-to h2 {
      font-size: 1.125rem;
      font-weight: 600;
      color: #c95b1f;
      margin-bottom: 0.75rem;
    }

    .bill-to-details {
      font-size: 0.875rem;
      line-height: 1.6;
    }

    .bill-to-details p {
      margin-bottom: 0.25rem;
    }

    .bill-to-details .font-semibold {
      font-weight: 600;
    }

    .bill-to-details .mt-2 {
      margin-top: 0.5rem;
    }

    .items-table {
      margin-bottom: 3rem;
      width: 100%;
      border-collapse: collapse;
    }

    .items-table thead tr {
      border-bottom: 2px solid #c95b1f;
    }

    .items-table th {
      padding: 0.75rem 0.5rem;
      font-weight: 600;
      text-align: left;
    }

    .items-table th.text-right {
      text-align: right;
    }

    .items-table tbody tr {
      border-bottom: 1px solid #e5e7eb;
    }

    .items-table td {
      padding: 1rem 0.5rem;
    }

    .items-table td.text-right {
      text-align: right;
    }

    .items-table td.font-semibold {
      font-weight: 600;
    }

    .totals-section {
      display: flex;
      justify-content: flex-end;
      margin-bottom: 3rem;
    }

    .totals-box {
      width: 20rem;
      font-size: 0.875rem;
    }

    .totals-row {
      display: flex;
      justify-content: space-between;
      padding: 0.5rem 0;
      border-top: 1px solid #d1d5db;
    }

    .totals-row:first-child {
      border-top-color: #d1d5db;
    }

    .totals-row .font-semibold {
      font-weight: 600;
    }

    .totals-total {
      display: flex;
      justify-content: space-between;
      padding: 0.75rem 0;
      border-top: 2px solid #c95b1f;
      font-size: 1.25rem;
      font-weight: 700;
      color: #c95b1f;
      margin-top: 0.25rem;
    }

    .payment-terms {
      margin-top: 4rem;
      padding-top: 2rem;
      border-top: 1px solid #e5e7eb;
      font-size: 0.875rem;
      color: #6b7280;
    }

    .payment-terms p {
      margin-bottom: 0.5rem;
    }

    .footer {
      margin-top: 2rem;
      text-align: center;
      font-size: 0.875rem;
      color: #374151;
    }

    /* Print Styles */
    @media print {
      @page {
        size: A4 portrait;
        margin: 0;
      }

      body {
        margin: 0;
        padding: 0;
        background: white;
      }

      .page-container {
        padding: 0;
        background: white;
      }

      .invoice-page {
        width: 210mm;
        height: 297mm;
        margin: 0;
        padding: 20mm;
        box-shadow: none !important;
        page-break-after: always;
      }

      /* Ensure colors print correctly */
      * {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
        color-adjust: exact !important;
      }

      /* Prevent page breaks inside elements */
      .header,
      .invoice-meta-row,
      .bill-to,
      .items-table,
      .totals-section,
      .payment-terms,
      .footer {
        page-break-inside: avoid;
      }

      /* Ensure table doesn't break poorly */
      .items-table tr {
        page-break-inside: avoid;
      }

      /* Ensure logo prints at correct size */
      .logo {
        width: 12rem;
        height: auto;
      }

      /* Maintain all colors */
      .invoice-title,
      .company-info h2,
      .bill-to h2,
      .totals-total {
        color: #c95b1f !important;
      }

      .invoice-meta-row {
        border-bottom-color: #e89264 !important;
      }

      .items-table thead tr {
        border-bottom-color: #c95b1f !important;
      }

      .totals-total {
        border-top-color: #c95b1f !important;
      }
    }
  </style>
</head>
<body>
  <div class="page-container">
    <div class="invoice-page">
      <!-- Header with Logo and Company Info -->
      <div class="header">
        <div class="company-info">
          <img 
            src="https://quizis.nl/images/custom/logo_quizis_large.png" 
            alt="Quizis Logo" 
            class="logo"
          />
          <h2>Factuuradres:</h2>
          <div class="bill-to-details">
            <p class="font-semibold"><?php echo $this->escapeHtml($customer->getName()); ?></p>
            <?php if ($customer->getAddress()): ?>
              <p><?php echo $this->escapeHtml($customer->getAddress()); ?></p>
            <?php endif; ?>
            <?php if ($customer->getPostalCode() || $customer->getCity()): ?>
              <p>
                <?php echo $this->escapeHtml($customer->getPostalCode()); ?> 
                <?php echo $this->escapeHtml($customer->getCity()); ?>
              </p>
            <?php endif; ?>
            <p>Nederland</p>
          </div>
        </div>
        
        <div class="invoice-title-section">
          <h1 class="invoice-title">FACTUUR</h1>
          <div class="company-details">
            <p class="font-semibold">Quizis B.V.</p>
            <p>Meester Bakxstraat 17</p>
            <p>5688SZ Oirschot</p>
            <p>Nederland</p>
            <p class="mt-3">BTW: NL857614423B01</p>
            <p class="mt-3">KVK: 68841396</p>
            <p class="mt-2">IBAN: NL41BUNQ2033722279</p>
          </div>
        </div>
      </div>

      <!-- Invoice Meta Row -->
      <div class="invoice-meta-row">
        <div>
          <span class="font-semibold">Factuurnummer:</span>
          <span><?php echo $this->escapeHtml($invoiceData['invoiceNumber']); ?></span>
        </div>
        <div>
          <span class="font-semibold">Datum:</span>
          <span><?php echo $invoiceData['invoiceDate']->format('d-m-Y'); ?></span>
        </div>
        <div>
          <span class="font-semibold">Vervaldatum:</span>
          <span><?php echo $invoiceData['dueDate']->format('d-m-Y'); ?></span>
        </div>
      </div>

      <!-- Quiz Items Table -->
      <div>
        <table class="items-table">
          <?php if ($invoiceData['pricingModel'] === 'fixed'): ?>
            <!-- Quiz on basis of priceInvoice -->
            <thead>
              <tr>
                <th>Omschrijving</th>
                <th class="text-right">Prijs</th>
                <th class="text-right">Totaal</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td><?php 
                    $quizName = $quiz->getName();
                    if (stripos($quizName, 'pubquiz') !== 0) {
                        $quizName = 'Pubquiz - ' . $quizName;
                    }
                    echo $this->escapeHtml($quizName);
                ?></td>
                <td class="text-right">€ <?php echo number_format($quiz->getPriceInvoice(), 2, ',', '.'); ?></td>
                <td class="text-right font-semibold">€ <?php echo number_format($quiz->getPriceInvoice(), 2, ',', '.'); ?></td>
              </tr>
            </tbody>
          <?php elseif ($invoiceData['pricingModel'] === 'perPerson'): ?>
            <!-- Quiz on basis of pricePerPersonInvoice -->
            <thead>
              <tr>
                <th>Omschrijving</th>
                <th class="text-right">Deelnemers</th>
                <th class="text-right">Prijs pp</th>
                <th class="text-right">Totaal</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td><?php 
                    $quizName = $quiz->getName();
                    if (stripos($quizName, 'pubquiz') !== 0) {
                        $quizName = 'Pubquiz - ' . $quizName;
                    }
                    echo $this->escapeHtml($quizName);
                ?></td>
                <td class="text-right"><?php echo $invoiceData['numberOfParticipants']; ?></td>
                <td class="text-right">€ <?php echo number_format($invoiceData['pricePerPerson'], 2, ',', '.'); ?></td>
                <td class="text-right font-semibold">€ <?php echo number_format($invoiceData['subtotal'], 2, ',', '.'); ?></td>
              </tr>
            </tbody>
          <?php endif; ?>
        </table>
      </div>

      <!-- Totals -->
      <div class="totals-section">
        <div class="totals-box">
          <div class="totals-row">
            <span>Subtotaal:</span>
            <span class="font-semibold">€ <?php echo number_format($invoiceData['subtotal'], 2, ',', '.'); ?></span>
          </div>
          <?php if ($invoiceData['discount'] > 0): ?>
            <div class="totals-row">
              <span>
                Korting
                <?php if ($quiz->getDiscountPercentage()): ?>
                  (<?php echo number_format($quiz->getDiscountPercentage(), 2, ',', '.'); ?>%)
                <?php endif; ?>:
              </span>
              <span class="font-semibold">- € <?php echo number_format($invoiceData['discount'], 2, ',', '.'); ?></span>
            </div>
            <div class="totals-row">
              <span>Subtotaal na korting:</span>
              <span class="font-semibold">€ <?php echo number_format($invoiceData['subtotalAfterDiscount'], 2, ',', '.'); ?></span>
            </div>
          <?php endif; ?>
          <div class="totals-row">
            <span>BTW (<?php echo $invoiceData['vatPercentage']; ?>%):</span>
            <span class="font-semibold">€ <?php echo number_format($invoiceData['vatAmount'], 2, ',', '.'); ?></span>
          </div>
          <div class="totals-total">
            <span>Totaal:</span>
            <span>€ <?php echo number_format($invoiceData['total'], 2, ',', '.'); ?></span>
          </div>
        </div>
      </div>

      <!-- Payment Terms -->
      <div class="payment-terms">
        <p>Gelieve het verschuldigde bedrag voor de vervaldatum over te maken naar het vermelde rekeningnummer onder vermelding van het factuurnummer.</p>
      </div>

      <!-- Footer -->
      <div class="footer">
        <p>Quizis B.V. | IBAN: NL41BUNQ2033722279 | info@quizis.nl | www.quizis.nl</p>
      </div>
    </div>
  </div>
</body>
</html>
